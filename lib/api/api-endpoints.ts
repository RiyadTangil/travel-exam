/**
 * Centralized API endpoints and Query Keys for the platform.
 */

export const ENDPOINTS = {
  CANDIDATES: { URL: "/api/candidates", KEY: "candidates" },
  STAFF: { URL: "/api/staff", KEY: "staff" },
  UPLOAD: { URL: "/api/upload", KEY: "upload" },
};
